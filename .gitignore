# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# 生ログ（Teams / Backlog 等の社内・クライアント情報を含むため公開しない）
_attachments/

# エディタ / OS
.idea/
.vscode/
*.swp
*~
Thumbs.db

# Obsidian（将来Vault化する際のローカル設定）
.obsidian/

# Claude Code のローカル設定（マシン固有・共有しない）
.claude/settings.local.json

# 生成物（.md がソース。HTML/PDF エクスポートは追跡しない。実名入りPDFの公開防止も兼ねる）
# 月次PDF/HTMLは monthly/YYYY/exports/ に出力し、フォルダごと追跡しない
exports/
*.html
*.pdf
